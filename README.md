*WebAuthn implemented using the library*
https://github.com/lbuchs/WebAuthn

Run the project
`docker-compose up`

Install dependencies in PHP container
`composer install`

Create the bundle in NPM container
`npm run bundle`